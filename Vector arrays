#include <iostream>  // 1. Найти максимальное значение массива.
#include <vector>
#include <algorithm>
using namespace std;

int main(){
    int numbers;
    cin >> numbers;
    std::vector<int> vectors;
    for (int i = 0; i < numbers; i++) {
		vectors.push_back(i + 1); 
    }
    auto result = max_element(vectors.begin(), vectors.end()); // поиск макс элемента
    cout << *result;
    vectors.clear();
  
    
}
